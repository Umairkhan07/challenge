import React from "react";

const Loading = () => {
  return (
    <div className="w-full flex items-center justify-center mt-10">
      <h1 className="font-semibold animate-loading">
        Loading Feeds, please wait
      </h1>
    </div>
  );
};

export default Loading;
